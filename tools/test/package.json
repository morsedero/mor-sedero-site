{
  "name": "test",
  "version": "1.0.0",
  "description": "Drives the real `tools/daisey.html` in Chromium against a stubbed connector\r bridge that replays the request/response shapes observed from live Trello and\r Google Calendar calls. No network, no real writes.",
  "main": "assert.js",
  "scripts": {
    "test": "node run-all.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
