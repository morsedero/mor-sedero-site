# Local Netlify folder
.netlify

# Raw/original uploads not needed in the repo (large, unprocessed source files)
Files for upload/

# Generated by tools/build-standalone.js. Kept OUT of the repo: the build now
# writes daisey/index.html directly, and Netlify runs it at deploy time, so
# this intermediate no longer exists in a normal build. Only older local
# checkouts still have one lying around.
tools/daisey-standalone.html
